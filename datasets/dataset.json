[
    {
        "task": "Parse an AWS S3 bucket name from an S3 URI (e.g., s3://my-bucket/path/to/file.txt)",
        "format": "regex"
    },
    {
        "task": "Create a JSON CloudFormation template for an EC2 security group that allows inbound HTTP and HTTPS traffic",
        "format": "json"
    },
    {
        "task": "Write a Python function that extracts the region from an AWS ARN string",
        "format": "python"
    },
    {
        "task": "Create a JSON policy document that grants read-only access to all objects in an S3 bucket",
        "format": "json"
    },
    {
        "task": "Write a Python function that validates if a string is a valid AWS EC2 instance ID (format: i-xxxxxxxxx)",
        "format": "python"
    }
]